[build]
builder = "railpack"

[phases.install]
cmds = [
  "composer install --no-dev --optimize-autoloader",
  "npm ci"
]

[phases.build]
# REMOVED config:cache and route:cache from here
cmds = [
  "npm run build"
]

[deploy]
# All caching happens here AT RUNTIME so environment variables are fresh
startCommand = "php artisan config:clear && php artisan route:clear && php artisan config:cache && php artisan route:cache && php artisan view:cache && php artisan migrate --force && (php artisan queue:work --tries=3 --timeout=90 & frankenphp php-server --listen :$PORT)"